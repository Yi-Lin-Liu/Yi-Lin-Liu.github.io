<style>
  .heartbeat {
    animation: pulse 1.5s infinite;
    text-align: center;
    color: #2c3e50;
  }
  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
  }
  .wave-bg {
    /* 這裡之後會放海浪的 CSS 動畫 */
    background: linear-gradient(180deg, #e0f7fa 0%, #81d4fa 100%);
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
</style>

<div class="wave-bg">
  <div class="heartbeat">
    <h1>以聲響為脈搏 以身體為媒介</h1>
    <p>點擊空間，開啟節奏之旅</p>
    <button onclick="playMusic()">探索生命</button>
  </div>
</div>
